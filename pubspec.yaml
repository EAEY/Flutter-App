name: student_hub_app
version: 1.0.0+1
description: A Flutter application for managing student courses.

publish_to: 'none'

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15

flutter:
  uses-material-design: true
  assets:
    - assets/images/profile.png